import 'package:flutter/material.dart';
import 'package:working_with_lists/models/story.dart';

class StoryTile extends StatelessWidget {
  const StoryTile({
    Key? key,
    required this.story,
  }) : super(key: key);

  final Story story;

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
